import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
    marginRight: 0,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginLeft: 0,
    },
    chart: {
      marginVertical: 0,
      borderRadius: 16,
    },
    chartTitle: {
      fontSize: 18,
      fontWeight: 'bold',
      marginBottom: 10,
    },
  });
  
  export {styles};